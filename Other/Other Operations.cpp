PRODUCTO CRUZ
inline ll cross ( ll x1, ll y1, ll x2, ll y2 ) {
	return x1*y2 - x2*y1;
}

PARA REEMPAZAR SCANF
ios_base::sync_with_stdio(0);
    cin.tie(0);

DEFINIR CANTIDAD DE DECIMALES PARA DOUBLE
cout.precision(6);
cout<<fixed<<euc<<endl<<fixed<<tax<<endl;

CONTAR 1s:

    int ans = __builtin_popcountll(sal);
    ll es por el long long

PRINTF:
    printf("%d\n",ans);

IFSTREAM:

ifstream fi("namenum.in");
    fi >> a;
